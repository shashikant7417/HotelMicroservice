server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotelms
    username: postgres
    password: Root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
